# Generated by c8.
coverage/

# The managed block is written verbatim by `nrstd sync` and marked "do not edit", but it is
# not Prettier-clean (no blank line after the `## Shared:` headings). Formatting it locally
# would make `sync` revert it on the next run and flip format:check back to red, so the file
# is left exactly as the standard emits it.
AGENTS.md

# Generated / lockfile — npm owns the formatting.
package-lock.json

# Runs on the Shelly device's mJS runtime, and ble-shelly-blu.js is vendored
# (Apache 2.0, ALLTERCO/shelly-script-examples). Do not reformat.
shelly/scripts/

# Node-RED flow exports — machine-generated, imported verbatim by users.
examples/

# The device catalog is hand-maintained data with deliberate column alignment, and
# adding a device is primarily an edit to this file. Prettier explodes every entry
# and every prefix list that exceeds printWidth onto its own line (165 -> 1110
# lines), which destroys the tabular layout that makes a catalog diff reviewable.
shelly/config/config.json
